3rd
//to find the volume of cuboid 
#include <stdio.h>
    void main()
{
float l,b,h,volume;
	printf("enter the value of l,b and h:");
	scanf("%f%f%f",&l,&b,&h);
    volume=l*b*h;
	printf("volume: %f",volume);
}
      

4th
//to find the volume of cuboid 
#include <stdio.h>
    void main()
{
float pi,r,volume;
	printf("enter the value of pi,r and volume:");
	scanf("%f",&r);
    volume=1.33*pi*r*r*r;
	printf("volume: %f",volume);
}

5th
#include <stdio.h>

int main()
{
    float a, perimeter, area;

    printf("Enter the value of side: ");
    scanf("%f", &a);

    perimeter = 4 * a;
    area = a * a;

    printf("Perimeter = %f\n", perimeter);
    printf("Area = %f\n", area);

    return 0;
}





//  write a program to check a given no. is even or odd 
#include<stdio.h>
void main()
{
 int n ;
    printf(" enter a number");
    scanf("%d", &n);
   if(n%2==0)
{
    printf( "n is even");
}
else
{
    printf("n is odd");
}


}